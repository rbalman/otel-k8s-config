# Part 1 — Application Deployment

See the **[otel-app repository README](https://github.com/rbalman/otel-app)** — it covers the Dockerfile, Helm chart, Ingress config, and probe setup (`/health`, `/ready`).
